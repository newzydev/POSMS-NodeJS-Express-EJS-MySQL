<!DOCTYPE html>
<html lang="th">

<head>
    <!-- Head -->
    <%- include('Include/Head.ejs') %>
    <!-- Head End -->
</head>

<body class="hold-transition login-page">
    
    <!-- Preloader -->
    <%- include('Include/Preloader.ejs') %>
    <!-- Preloader End -->

    <!-- Content Wrapper. Contains page content -->
    <div class="login-box">
        <div class="card">
            <div class="card-header text-center">
                <img src="/assets/images/logo/logo_icon.png" alt="Logo Point of Sale Management System" width="15%">
            </div>
            <div class="card-body login-card-body">
                <p class="login-box-msg">ยินดีต้อนรับเข้าสู่ <b><%= settings.store_name %></b><br>ยืนยันที่อยู่อีเมล์</p>

                <% if (error) { %>
                <div class="alert alert-danger text-center" role="alert">
                    <span><i class="fas fa-exclamation-triangle mr-2"></i><%= error %>
                </div>
                <% } %>
                <% if (success) { %>
                <div class="alert alert-success text-center" role="alert">
                    <span><i class="fas fa-check-circle mr-2"></i><%= success %>
                </div>
                <% } %>
                
                <form class="needs-validation" novalidate id="login_form" method="POST">
                    <input type="hidden" name="member_firstname" value="<%= customer.member_firstname %>">
                    <input type="hidden" name="member_lastname" value="<%= customer.member_lastname %>">
                    <input type="hidden" name="member_email" value="<%= customer.member_email %>">
                    <input type="hidden" name="member_email_activate" value="<%= customer.member_email_activate %>">
                    <div class="row">
                        <div class="col-12">
                            <label for="code_6_digit" class="required">รหัสยืนยัน 6 หลัก</label>
                            <input type="text" class="form-control form-control-lg" name="code_6_digit" id="code_6_digit" placeholder="รหัสยืนยัน 6 หลัก" autocomplete="off" required>
                            <div class="valid-feedback">
                                กรอกรหัสยืนยัน 6 หลักถูกต้อง
                            </div>
                            <div class="invalid-feedback">
                                กรุณากรอกรหัสยืนยัน 6 หลักให้ถูกต้อง
                            </div>
                        </div>
                    </div>
                    <hr>
                    <div class="row">
                        <div class="col-12 mb-3">
                            <button type="submit" id="btn_submit" class="btn btn-custom-one btn-lg btn-block">ยืนยันที่อยู่อีเมล์</button>
                        </div>
                    </div>
                    <div class="text-center btn btn-custom-two btn-lg btn-block btn-anti" style="cursor: pointer;" onclick="location.href='/';">ย้อนกลับ</div>
                </form>                

            </div>
        </div>
    </div>
    <!-- Content Wrapper. Contains page content End -->

    <!-- Javascript -->
    <%- include('Include/Javascript.ejs') %>
    <!-- Javascript End -->
</body>
</html>