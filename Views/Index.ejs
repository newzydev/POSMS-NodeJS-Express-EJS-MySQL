<!DOCTYPE html>
<html lang="th">

<head>
    <%- include('Include/Head.ejs') %>
</head>

<body class="hold-transition login-page">
    <%- include('Include/Preloader.ejs') %>
    <div class="login-box">
        <div class="card">
            <div class="card-header text-center">
                <img src="/assets/images/logo/logo_icon.png" alt="Logo Point of Sale Management System" width="15%">
            </div>
            <div class="card-body login-card-body">
                <div class="login-box-msg">
                    ยินดีต้อนรับเข้าสู่ <b><%= settings.store_name %></b><br>
                    ลงชื่อเข้าใช้ระบบเพื่อเริ่มเซสชั่นของคุณ<br>
                    ป้อน <b>อีเมล์</b> และ <b>หมายเลขโทรศัพท์</b>
                </div>
                
                <% if (error) { %>
                <div class="alert alert-danger text-center" role="alert">
                    <span><i class="fas fa-exclamation-triangle mr-2"></i><%= error %>
                </div>
                <% } %>
                <% if (success) { %>
                <div class="alert alert-success text-center" role="alert">
                    <span><i class="fas fa-check-circle mr-2"></i><%= success %>
                </div>
                <% } %>
                
                <form class="needs-validation" novalidate id="login_form" method="POST">
                    <div class="row">
                        <div class="col-12 mb-3">
                            <label for="member_email" class="required">ที่อยู่อีเมล์</label>
                            <input type="email" class="form-control form-control-lg" name="member_email" id="member_email" placeholder="ที่อยู่อีเมล์" value="<%= formData ? formData.member_email : '' %>" autofocus autocomplete="off" required>
                            <div class="valid-feedback">
                                กรอกที่อยู่อีเมล์ถูกต้อง
                            </div>
                            <div class="invalid-feedback">
                                กรุณากรอกที่อยู่อีเมล์ให้ถูกต้อง
                            </div>
                        </div>
                        <div class="col-12">
                            <label for="member_tel" class="required">หมายเลขโทรศัพท์</label>
                            <input type="tel" class="form-control form-control-lg" name="member_tel" id="member_tel" placeholder="หมายเลขโทรศัพท์" value="<%= formData ? formData.member_tel : '' %>" autofocus autocomplete="off" required>
                            <div class="valid-feedback">
                                กรอกหมายเลขโทรศัพท์ถูกต้อง
                            </div>
                            <div class="invalid-feedback">
                                กรุณากรอกหมายเลขโทรศัพท์ให้ถูกต้อง
                            </div>
                        </div>
                    </div>
                    <hr>
                    <div class="row">
                        <div class="col-12 mb-3">
                            <button type="submit" id="login_submit" class="btn btn-custom-one btn-lg btn-anti btn-block">ลงชื่อเข้าใช้</button>
                        </div>
                        <div class="col-12 mb-3">
                            <div class="btn btn-custom-two btn-lg btn-anti btn-block" onclick="location.href='/Register';">สมัครสมาชิกสำหรับลูกค้า</div>
                        </div>
                        <div class="col-12">
                            <div class="btn btn-lg btn-block" data-toggle="modal" data-target="#Store_Information"><i class="fas fa-info-circle mr-2"></i>เกี่ยวกับร้านค้า</div>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <%- include('Include/Javascript.ejs') %>
</body>

</html>